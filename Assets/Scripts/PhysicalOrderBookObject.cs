using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PhysicalOrderBookObject : MonoBehaviour
{

    public float height;
    public int idx;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void SetupOrderBookObject(int idx) {
        //using idx get height from api call
        //height = call.order.amount;

        //call manager with idx to get x-z positioning on plane

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
